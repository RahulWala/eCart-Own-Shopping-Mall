extends base

block content
	table.table
		thead
			tr
				th Products
				th Price
				th Delete
		tbody
			for item in items
				tr
					td #{item.proName}
					td #{item.price}
					td
						form(method="post", action="http://localhost:3000/users/delete/#{item._id}")
							button(type='submit') Delete